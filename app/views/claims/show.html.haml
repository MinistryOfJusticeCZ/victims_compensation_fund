.claim
  .attribute
    .label= Claim.human_attribute_name('file_uid')
    .value= @claim.file_uid
- if can? :read, Appeal
  .appeals
    %h4= Appeal.model_name.human(count: :other)
    #appeals_grid
- if can? :read, Redemption
  .redemptions
    %h4= Redemption.model_name.human(count: :other)
    #redemptions_grid

:javascript
  #{raw render 'common/grid', grid_id: 'appeals_grid', schema: @appeals_schema, create_attributes: {claim_id: @claim.id}}
  #{raw render 'common/grid', grid_id: 'redemptions_grid', schema: @redemptions_schema, create_attributes: {claim_id: @claim.id}}
